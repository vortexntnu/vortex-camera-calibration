#!/usr/bin/env python3
"""Thin alias for main.py, kept so `./calib ...` in the README still works."""
import pathlib
import runpy
import sys

sys.argv[0] = str(pathlib.Path(__file__).resolve().parent / "main.py")
runpy.run_path(sys.argv[0], run_name="__main__")
